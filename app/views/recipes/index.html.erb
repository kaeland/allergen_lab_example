<h1>Our Recipes</h1>

<%= link_to "Create a New Recipe", new_recipe_path %>

<ul>
  <% @recipes.each do |recipe| %>
    <li>Title: <%= link_to recipe.title, recipe_path(recipe) %></li>  
  <% end %>
</ul>

